services:
  - type: web
    name: ayurherb-backend
    env: node
    region: oregon
    rootDirectory: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
      - key: FRONTEND_URL
        value: https://ayurherb-6pkcdz62z-junaid-mollahs-projects.vercel.app
